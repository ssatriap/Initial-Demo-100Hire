# Initial Demo 100Hire

Initial Demo 100Hire is a starter repository for building and iterating on the 100Hire demo project.

At the moment, this repository is in a bootstrap state (version control is initialized, but application source files have not been added yet). This README provides a clear starting point so contributors can set up the project consistently as development begins.

## Project Description

This project is intended to serve as the foundation for the 100Hire demo application. The repository is prepared for collaborative development and can be used to:

- add core application code and supporting services,
- define environment and dependency requirements,
- establish development, testing, and deployment workflows.

As implementation is added, this README should be updated with stack-specific details (framework, database, architecture, and API or UI behavior).

### Tools Installed

- Cursor Apps
- Claude Code add-on
- Codex add-on

### Steps Completed

1. Registered on GitHub with username `ssatriap`.
2. Created the first repository on GitHub.
3. Connected Cursor and GitHub accounts so Cursor can open the GitHub repository.
4. Created this `README.md` as the first task.

### Issues

There were no major issues during the task. The only slightly confusing step was choosing the right add-ons, so the most downloaded options were selected for Claude Code and Codex.

## Installation

Follow these steps to set up the project locally.

### 1) Clone the repository

```bash
git clone <your-repository-url>
cd "Initial Demo 100Hire"
```

### 2) Confirm toolchain

Install and verify the tools your team plans to use (for example: Node.js, Python, Docker, etc.).

```bash
git --version
```

### 3) Create project files

Because this repository is currently empty (except Git metadata), add your initial project scaffold before running an application:

- initialize your chosen runtime/framework,
- add dependency manifest files (for example, `package.json` or `pyproject.toml`),
- define environment configuration files (for example, `.env.example`).

## Usage Instructions

Usage will depend on the technology stack you add. Use this baseline workflow until stack-specific commands are defined.

### Development workflow

1. Pull the latest changes:

   ```bash
   git pull
   ```

2. Add or update project files in the repository root.
3. Run your stack's install command (for example, `npm install` or `pip install -r requirements.txt`).
4. Start your app using the framework's development command.
5. Commit and push your changes.

### Suggested command sections to add later

Once the codebase is scaffolded, replace this section with concrete commands such as:

- **Install dependencies**: `...`
- **Run development server**: `...`
- **Run tests**: `...`
- **Build for production**: `...`

## Repository Status

Current status:

- Git repository initialized
- No application source files committed yet
- README scaffold created for onboarding

## Contributing

1. Create a feature branch from your main branch.
2. Make focused, reviewable changes.
3. Update this README when adding new setup or runtime commands.
4. Open a pull request with a clear summary and test notes.

## License

Add your project license details here (for example, MIT, Apache-2.0, or proprietary).

